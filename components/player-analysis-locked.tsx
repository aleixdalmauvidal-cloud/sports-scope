/**
 * Previously showed a sign-in / paywall overlay. Player analysis is now always visible
 * where `PlayerProfileAnalysis` (or equivalent) is rendered; this export remains as a no-op
 * so imports do not break.
 */
export function PlayerAnalysisLocked() {
  return null;
}
